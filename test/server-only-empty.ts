// Empty stub for the `server-only` marker package.
// Next.js resolves `server-only` to an empty module via the `react-server`
// export condition during its build. Plain tsx test runs don't apply that
// condition, so we map `server-only` to this file via tsconfig paths.
export {};
